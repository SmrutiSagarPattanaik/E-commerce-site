import { FilterItemDetailsPipe } from './filter-item-details.pipe';

describe('FilterItemDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
