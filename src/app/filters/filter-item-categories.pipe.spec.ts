import { FilterItemCategoriesPipe } from './filter-item-categories.pipe';

describe('FilterItemCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
